crate: esp-radio-preempt-baremetal

options:
  - name: tick_rate_hz
    description: 'Tick rate of the task scheduler in Hertz'
    default:
      - value: 100
    constraints:
      - type:
          validator: positive_integer
