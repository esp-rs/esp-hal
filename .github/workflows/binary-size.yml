name: Binary Size Analysis

on:
  workflow_dispatch:
    inputs:
      pr_number:
        description: "Pull request number to analyze"
        required: true
        type: string
      example_name:
        description: "Example directory name to analyze (e.g., embassy_dhcp)"
        required: true
        type: string

jobs:
  binary-size:
    runs-on: ubuntu-latest
    permissions:
      actions: read
      pull-requests: write
      contents: read

    strategy:
      fail-fast: false
      matrix:
        target:
          - soc: esp32c3
            rust-target: riscv32imc-unknown-none-elf
          - soc: esp32c6
            rust-target: riscv32imac-unknown-none-elf
          - soc: esp32
            rust-target: xtensa-esp32-none-elf

    steps:
      - name: Setup Rust
        # Install the Rust toolchain for RISC-V devices:
        if: ${{ !contains(fromJson('["esp32", "esp32s2", "esp32s3"]'), matrix.target.soc) }}
        uses: dtolnay/rust-toolchain@v1
        with:
          target: ${{ matrix.target.rust-target }}
          toolchain: stable
          components: rust-src, llvm-tools

      # Install the Rust toolchain for Xtensa devices:
      - if: contains(fromJson('["esp32", "esp32s2", "esp32s3"]'), matrix.target.soc)
        uses: esp-rs/xtensa-toolchain@v1.6
        with:
          buildtargets: ${{ matrix.target.soc }}
          default: true
          version: 1.93.0.0

      - name: Install Binutils and Hub
        run: |
          cargo install cargo-binutils
          sudo apt-get update
          sudo apt-get install -y hub

      # Checkout repo & PR
      - name: Checkout Repo (Initial)
        uses: actions/checkout@v6

      - name: Checkout Pull Request (PR Code at Root)
        # context https://github.com/actions/checkout/issues/331
        run: hub pr checkout ${{ github.event.inputs.pr_number }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Cache Cargo Dependencies
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/
            ~/.cargo/git/
            ./target/
          key: ${{ runner.os }}-${{ matrix.target.soc }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Dir for result files
        run: |
          cd $HOME
          mkdir results

      - name: Build PR example
        run: |
          set -euo pipefail

          RAW_INPUT="${{ github.event.inputs.example_name }}"
          cargo xtask build examples $RAW_INPUT --chip "${{ matrix.target.soc }}"

          EXAMPLE_NAME=$(echo "$RAW_INPUT" | awk '{print $1}')
          BIN_NAME=$(echo "$EXAMPLE_NAME" | tr '_' '-')

          BIN=$(
          find target qa-test/target examples -type f \
            -path "*/release/${BIN_NAME}" \
            -executable \
            2>/dev/null | head -n 1 || true
          )

          # Fallback to `_` instead of `-`
          if [ -z "$BIN" ]; then
          BIN=$(find target qa-test/target examples -type f \
            -path "*/release/${EXAMPLE_NAME}" \
            -executable \
            2>/dev/null | head -n 1 || true)
          fi

          if [ -z "$BIN" ]; then
            echo "Error: Built PR binary not found"
            exit 1
          fi

          cp "$BIN" $HOME/results/pr_qa_build_${{ matrix.target.soc }}.elf

      # Checkout base
      - name: Checkout Base Commit
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Build Base example
        run: |
          set -euo pipefail
          cargo clean

          RAW_INPUT="${{ github.event.inputs.example_name }}"
          cargo xtask build examples $RAW_INPUT --chip "${{ matrix.target.soc }}"

          EXAMPLE_NAME=$(echo "$RAW_INPUT" | awk '{print $1}')
          BIN_NAME=$(echo "$EXAMPLE_NAME" | tr '_' '-')

          BIN=$(
          find target qa-test/target examples -type f \
            -path "*/release/${BIN_NAME}" \
            -executable \
            2>/dev/null | head -n 1 || true
          )

          # Fallback to `_` instead of `-`
          if [ -z "$BIN" ]; then
          BIN=$(find target qa-test/target examples -type f \
            -path "*/release/${EXAMPLE_NAME}" \
            -executable \
            2>/dev/null | head -n 1 || true)
          fi

          if [ -z "$BIN" ]; then
            echo "Error: Built BASE binary not found"
            exit 1
          fi

          cp "$BIN" $HOME/results/base_qa_build_${{ matrix.target.soc }}.elf

      - name: Copy binaries to workspace for Bloaty
        run: |
          # Copy files from $HOME/results to current workspace
          cp $HOME/results/base_qa_build_${{ matrix.target.soc }}.elf ./
          cp $HOME/results/pr_qa_build_${{ matrix.target.soc }}.elf ./
          ls -la *.elf

      # Diffs and Artifacts
      - name: Run Bloaty QA Diff
        id: bloaty-qa
        uses: carlosperate/bloaty-action@v1
        with:
          bloaty-args: -d sections --domain vm --source-filter "^.text$|^.data|^.bss|^.rwdata|^.rwtext" pr_qa_build_${{ matrix.target.soc }}.elf -- base_qa_build_${{ matrix.target.soc }}.elf
          output-to-summary: false
        continue-on-error: true

      - name: Save Bloaty QA output
        run: |
          echo "QA_DIFF<<EOF" > result-pr-${{ matrix.target.soc }}.txt
          echo "\`\`\`" >> result-pr-${{ matrix.target.soc }}.txt
          echo "${{ steps.bloaty-qa.outputs.bloaty-output || 'N/A' }}" >> result-pr-${{ matrix.target.soc }}.txt
          echo "\`\`\`" >> result-pr-${{ matrix.target.soc }}.txt
          echo "EOF" >> result-pr-${{ matrix.target.soc }}.txt

      - name: Upload Result Artifact
        uses: actions/upload-artifact@v6
        with:
          name: result-${{ matrix.target.soc }}
          path: result-pr-${{ matrix.target.soc }}.txt

  report-results:
    needs: binary-size
    if: always()
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
      - uses: actions/download-artifact@v4
        with:
          pattern: result-*
          path: ./results
          merge-multiple: true

      - name: Generate Combined Report
        id: combine
        working-directory: ${{ github.workspace }}
        run: cargo run -p xtask --features report -- generate-report --input results

      - name: Find and Update Comment with GH CLI
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ISSUE_NUMBER=${{ github.event.inputs.pr_number }}
          echo "Processing PR Number: $ISSUE_NUMBER"

          COMMENTS=$(gh api repos/${{ github.repository }}/issues/$ISSUE_NUMBER/comments)

          # 1) Prefer the trigger comment ("Triggered binary size analysis ...")
          COMMENT_ID=$(echo "$COMMENTS" | jq -r '
            .[]
            | select(.user.login == "github-actions[bot]")
            | select(.body | contains("Triggered binary size analysis"))
            | .id' | head -n1)

          # 2) Fallback to an existing Binary Size Report comment (for older runs)
          if [ -z "$COMMENT_ID" ] || [ "$COMMENT_ID" = "null" ]; then
            COMMENT_ID=$(echo "$COMMENTS" | jq -r '
              .[]
              | select(.user.login == "github-actions[bot]")
              | select(.body | test("^## Binary Size Report"))
              | .id' | head -n1)
          fi

          if [ -n "$COMMENT_ID" ] && [ "$COMMENT_ID" != "null" ]; then
            echo "Updating existing comment: $COMMENT_ID"
            gh api -X PATCH \
              repos/${{ github.repository }}/issues/comments/$COMMENT_ID \
              -f "body=$(cat report.txt)"
          else
            echo "Creating new comment"
            gh api -X POST \
              repos/${{ github.repository }}/issues/$ISSUE_NUMBER/comments \
              -f "body=$(cat report.txt)"
          fi
